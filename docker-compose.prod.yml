services:
  backend:
    build:
      context: ./backend
      target: production
    environment:
      NODE_ENV: production
    volumes: !override
      - ./uploads:/data/uploads

  frontend:
    build:
      context: ./frontend
      target: production
    environment:
      NODE_ENV: production
    volumes: !reset []
